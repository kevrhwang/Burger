<img src="/img/burger.png" alt="">

<h1>Eat-Da-Burger!</h1>

<h2>Burgers to be devoured!</h2>
<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers eaten!</h2>
<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<form class="create-form">

  <div class="form-group">
  	<label for="burg">Enter a burger you want to eat then hit Submit.</label>
  	<br>
    <textarea rows='5' cols='50' type="text" id="burg" name="burger_name">Enter burger here...</textarea>
    
  </div>

  <button type="submit" id="submit">Submit</button>
</form>
